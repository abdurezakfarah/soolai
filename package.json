{
  "name": "soolai",
  "version": "1.0.0",
  "type": "module",
  "author": "Cabdirisaaq Faarax",
  "license": "MIT",
  "scripts": {
    "install": "concurrently \"cd client && yarn\" \"cd server && yarn\"",
    "build": "concurrently \"cd client && tsc && yarn vite build\" \"cd server && yarn build\"",
    "dev": "concurrently \"cd client && yarn dev\" \"cd server && yarn dev\"",
    "preview": "cd client && yarn vite preview",
    "serve": "cd client && yarn vite preview",
    "start": "node"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1",
    "tsx": "^3.12.8",
    "typescript": "^5.2.2"
  }
}
